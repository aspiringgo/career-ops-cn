# 中文工作流说明

## 第 1 步：提供简历

支持 `.pdf`、`.md`、`.txt`。程序会提取文本，并让所选 AI 模型整理为中文 Markdown。

## 第 2 步：提供岗位

使用 `--jd` 指定本地 JD，或者用 `--jd-url` 提供网页链接。网页会先提取可读文本，再由 AI 整理。招聘网站阻止抓取时，直接复制 JD 保存为 Markdown 文件。

## 第 3 步：AI 分析

AI 会输出：

- 0-100 综合匹配分
- 与 JD 证据对应的优势
- 缺少证据的能力缺口
- 简历改写和申请策略
- 中国招聘语境下的面试问题

AI 不应编造候选人的经历；无法确认的内容应标记为“待确认”。

## 第 4 步：人工审核

用户检查 `normalized/` 和 `reports/`，确认事实准确后再决定是否修改简历或申请岗位。
